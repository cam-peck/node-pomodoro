const data = {
    nIntervId: null,
    remainingTime: 0,
    currentPomoInterval: 'workTime',
    pomoIntervalCounter: 0
};

const config = {
    workInterval: 25,
    shortBreak: 5,
    longBreak: 30
};

export { data, config };