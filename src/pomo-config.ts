const pomoConfig: {
    workInterval: number,
    shortBreakInterval: number,
    longBreakInterval: number
} = {
    workInterval: 25,
    shortBreakInterval: 5,
    longBreakInterval: 30
};

export { pomoConfig };